# Dependencies
node_modules/

# Build output
client/dist/

# Live database and backups (do NOT publish) — match anywhere in tree
scoring.db
scoring.db-journal
scoring.db-wal
scoring.db-shm
data/backups/
data/tmp/

# Runtime data (DB, backups, uploaded meet logos) — not source
server/data/
data/

# Local Claude / editor / OS files
.claude/
.DS_Store
.vscode/
.idea/

# Environment files (secrets)
.env
.env.local
.env.*.local
server/.env

# Release zips
*.zip

# Stray root npm artifact (no real package.json at root)
/package-lock.json

# Logs
*.log
npm-debug.log*

# Mac Finder duplicates ("foo 2.json")
* [0-9].*

# Pi image build workspace (server/scripts/build_pi_image/build.sh)
.work/
