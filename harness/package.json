{
  "name": "stickit-harness",
  "version": "2.0.0",
  "private": true,
  "description": "StickIt v2 simulation test harness (R16). Standalone — run only on the development Mac; never deployed; excluded from the release zip.",
  "scripts": {
    "test": "node run.js",
    "install-browsers": "playwright install chromium"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0",
    "playwright": "^1.45.0",
    "uuid": "^9.0.0"
  }
}
